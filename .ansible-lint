---
profile: production

strict: true

extra_vars:
  scaffold_message: "Hello from the scaffold role"

mock_roles: []

exclude_paths:
  - .github/
  - molecule/
  - tests/

enable_list:
  - fqcn
  - no-changed-when
  - no-handler
  - yaml

warn_list:
  - role-name[match]

skip_list: []
